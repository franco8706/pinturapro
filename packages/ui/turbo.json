{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    // Solo hace typecheck (noEmit): no genera artefactos que cachear.
    "build": {
      "outputs": []
    }
  }
}
